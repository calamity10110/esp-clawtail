# ble_hid_actions

Internal BLE HID action schema and dispatch helpers.

This module is not a runtime entry point for agents. Agents should call scripts under `/fatfs/skills/ble_hid/scripts/` instead.
